# rsync exclude patterns, related to `--exclude-from=FILE` option
#
# insert below exclude patterns (one per line)
# read `man rsync` for other information

# file and folders that should not be included in rsync backups:
.bash_history
.cache/

# examples of exclude patterns
Downloads/
Desktop/
.backups/
.dbus/
.gvfs/
.local/share/gvfs-metadata/
.local/share/Trash/
.python_history
*.pid
.thumbnails/
*.tmp
